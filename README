board.tcl:
	TCL script for "expect" http://en.wikipedia.org/wiki/Expect.
	You need to use "board.tcl" for automatical perfomance operations:
	 - open ftp-connection with remote host, transmit files and close the connection
	 - open telnet-connection and perform "chmod 777" command for each exported file

	Usage: board.tcl <host> [<file> ...]
	 <host> remote host
	 <file> transfered file

timeout.py:
	Timeout extension for functions like IO
	functions (e.g. read, raw_input, input).
	For more information use "pydoc timeout".

flashdbg.install.sh:
	Adobe Flash Debugger Installer
